/*function LoginUser(userService)
{
        var vm=this;
    vm.user={}; 

    vm.loginUser=function(){
        var promise=userService.loginUser(vm.user);
            promise.then(function(result)
            {console.log('login User', result);
            });
    };
}
        
var component = {
    templateUrl: 'main/users/user/user.html',
    controller: loginUser
};
angular
.module('main')
.component('loginUser', component);
  */